apply from: '../config/common.gradle'

configureJavaModule(project, 'echart-workspace')

dependencies {
    implementation lib('echart-core')
    implementation lib('echart-ais')
    implementation lib('echart-route')
    testImplementation group: 'junit', name: 'junit', version: junitVersion
}

jar {
    archiveBaseName.set('echart-workspace')
    
    manifest {
        attributes 'Implementation-Title': 'E-Chart Workspace Library',
                   'Implementation-Version': archiveVersion.get()
    }
}
