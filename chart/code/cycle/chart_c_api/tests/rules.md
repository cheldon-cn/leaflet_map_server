对于code\cycle\chart_c_api\tests中涉及未实现API的测试用例，不直接删除，而是过滤+归档。
运行静态检查（如编译或语法解析）识别与当前SDK不匹配的测试用例。
将能通过编译/匹配API的测试用例保留并集成。
将不匹配的测试用例移动到 pending_tests/ 目录，并添加元数据（期望的API签名、生成时间、原因），不删除。

每个迭代评估 pending_tests 中的用例：如果缺失的API是合理的（符合产品规划），则优先完善SDK，然后重新激活对应测试。
如果发现测试用例明显错误（如调用不存在的类型），则直接丢弃或修正

额外建议
为SDK建立接口契约文档（如OpenAPI或手写头文件规范），用其约束大模型生成测试时的上下文，减少不一致